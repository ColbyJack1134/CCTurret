# Screenshots

Drop the following PNGs in this directory. The repo README references them by
these exact filenames:

| File | What to capture |
| --- | --- |
| `turret-firing.png` | Hero shot — the cannon tracking and firing on a target |
| `static-build.png` | A ground-mounted turret: the two speed controllers, block reader on the mount, player detector, relay, and computer |
| `ship-build.png` | A turret mounted on an airship, showing the nav table and gimbal sensor |
| `targets-tab.png` | The TARGETS tab with a roster (a `@player` and/or `#ship`) |
| `debug-tab.png` | The DEBUG tab showing live aim numbers (incl. heading / ship pitch / ship roll for the airship section) |
| `config-tab.png` | The CONFIG tab, showing the grouped settings rows |

Spruce-integration videos go in the README's **Spruce integration** section
(replace the `<!-- TODO -->` comment with the two links).
